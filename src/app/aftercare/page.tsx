import { DomainPage } from "@/components/domain-page";
import { domainContent } from "@/lib/mock-data";

export default function AftercarePage() {
  return <DomainPage content={domainContent.aftercare} />;
}